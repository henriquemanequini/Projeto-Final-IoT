# =============================================================================
# .env.example - Estacionamento Inteligente (v2 - AWS IoT Core + Timestream)
# =============================================================================
# Copie pra .env e preencha. NUNCA commite o .env real (esta no .gitignore).
# =============================================================================

# --- AWS (Timestream) ------------------------------------------------------
# Credenciais IAM fornecidas pelo professor no Blackboard
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1

# --- Timestream ------------------------------------------------------------
# A tabela ativa do prof eh SmartSpace.Ocupacao (tem 5k+ registros recentes).
# A iot-eletiva.iot-2025 esta vazia e e legado de outra aula.
TIMESTREAM_DB=SmartSpace
TIMESTREAM_TABL