# =============================================================================
# .env.example - Estacionamento Inteligente (v2 - AWS IoT Core + Timestream)
# =============================================================================
# Copie pra .env e preencha. NUNCA commite o .env real (esta no .gitignore).
# =============================================================================

# --- AWS (Timestream) ------------------------------------------------------
# Credenciais IAM fornecidas pelo professor no Blackboard
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1

# --- Timestream ------------------------------------------------------------
TIMESTREAM_DB=iot-eletiva
TIMESTREAM_TABLE=iot-2025

# --- AWS IoT Core (broker MQTT/TLS) ----------------------------------------
MQTT_BROKER=aspvpxjmfalxx-ats.iot.us-east-1.amazo